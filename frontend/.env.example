# CoFoundr Frontend Environment Variables Example
# Duplicate this file as .env.local and adjust keys for local deployment

# URL of the backend API service
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# URL of the WebSocket gateway
NEXT_PUBLIC_WS_URL=ws://localhost:8000
